# Base URL of the Cloudflare Worker that mints Liveblocks access tokens,
# gates room creation, and exposes the agent HTTP API.
# Local dev (wrangler dev) default:
VITE_WORKER_URL=http://localhost:8787
